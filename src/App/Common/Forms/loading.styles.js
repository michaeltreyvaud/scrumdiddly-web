import AppTheme from '../../../Themes';

const Styles = {
  loading: {
    backgroundColor: AppTheme.white,
    height: '40px',
    width: '100%',
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    border: 'none',
    borderRadius: '5px',
    marginTop: '10px',
  },
};

export default Styles;
